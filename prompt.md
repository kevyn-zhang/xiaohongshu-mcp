你是一个专业的小红书运营助手。请按照以下步骤，全自动帮我制作并发布一篇“口才训练”笔记。

### 任务参考
参考风格：口才训练/新闻复述类笔记。
风格特点：封面是大字标题，正文是具体的训练文案+技巧指导。

### 第一步：生成内容
请以“【每日口才挑战】提升气场”为主题，生成一篇内容：
1. **标题**：吸引眼球，包含 Emoji（如🎤、🔥），20字以内。
2. **训练素材**：生成一段约 150 字的“即兴评述”或“新闻播报”练习文案，要求逻辑清晰，朗朗上口。
3. **技巧指导**：列出 3 点具体的朗读技巧（如重音、停顿、气息）。
4. **标签**：#口才训练 #普通话 #播音主持 #自我提升

### 第二步：生成图片 (Python 绘图)
请编写并执行一段 Python 代码 (使用 PIL 库)，完成以下图片处理：
1. 读取本地目录下的背景图 `template.jpg` (假设位于当前工作区的 docker/images/ 目录下，如果找不到请提示我)。
2. 将第一步生成的“标题”以超大字号居中写在图片上方。
3. 将“训练素材”以适中字号自动换行写在图片中间。
4. 字体颜色使用黑色或深色，确保清晰。
5. **重要**：将生成的图片保存为 `final_post.jpg`，并保存在与 `template.jpg` 相同的目录下。
6. *注意：请检查环境中是否有中文字体，如果没有，请尝试使用系统默认中文字体路径 (如 C:/Windows/Fonts/msyh.ttc)。*

### 第三步：调用 MCP 发布 (Docker路径)
图片生成成功后，请调用 `xiaohongshu` MCP 工具进行上传：
1. **工具**：`publish_note` (或相应的发布工具)。
2. **标题**：使用第一步生成的标题。
3. **正文**：
   [标题]
   
   📅 今日训练打卡
   ----------------
   [训练素材]
   ----------------
   💡 练习技巧：
   [技巧指导]
   
   [标签]
4. **图片路径**：`["/app/images/final_post.jpg"]` 
   *(注意：这是 Docker 容器内的路径，对应本地的 final_post.jpg)*。

### 第四步：执行要求
- 在上传完毕后，**请不要点击最后的“发布”按钮**（如果工具支持 `save_draft` 参数请设为 true，或者在填完内容后停止操作），我想保留在草稿箱人工审核。
- 请一步步执行，先生成文案，确认无误后再生成图片，最后发布。